services:
    order:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: order
        ports:
            - "5000:5000"
        restart: on-failure
        volumes:
            - ./certs:/order/certs:ro